<Window x:Class="ChipLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chip Launcher"
        Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- 顶部标题栏 -->
        <Border Grid.Row="0" Grid.ColumnSpan="2"
                Background="{StaticResource SidebarBrush}">
            <TextBlock Text="Chip Launcher"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="{StaticResource TextPrimaryBrush}"
                       VerticalAlignment="Center"
                       Margin="20,0,0,0" />
        </Border>

        <!-- 左侧导航栏 -->
        <Border Grid.Row="1" Grid.Column="0"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,1,0">
            <StackPanel Background="{StaticResource SidebarBrush}">
                <Button x:Name="BtnPlay" Content="▶ 启动游戏"
                        Style="{StaticResource SidebarButtonStyle}" />
                <Button x:Name="BtnMods" Content="📦 模组管理"
                        Style="{StaticResource SidebarButtonStyle}" />
                <Button x:Name="BtnNews" Content="📰 游戏资讯"
                        Style="{StaticResource SidebarButtonStyle}" />
                <Button x:Name="BtnSettings" Content="⚙ 设置"
                        Style="{StaticResource SidebarButtonStyle}" />
            </StackPanel>
        </Border>

        <!-- 右侧内容区 -->
        <Frame x:Name="ContentFrame"
               Grid.Row="1" Grid.Column="1"
               NavigationUIVisibility="Hidden" />
    </Grid>
</Window>